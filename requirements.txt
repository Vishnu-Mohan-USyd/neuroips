# Model, training, and assays use PyTorch plus the Python standard library.
# The tracked reference-figure generator also requires Matplotlib.
# A CPU-only torch build works (the device auto-falls back to CPU); a CUDA build is
# recommended for training (forward-only assays are cheap on CPU).
# Tested with torch 2.x; the codebase uses Python >= 3.10 syntax.
torch>=2.0
matplotlib>=3.6
