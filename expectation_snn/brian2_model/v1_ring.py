"""V1 fixed feature-tuned ring.

12 orientation channels at 15 deg spacing over 0-165 deg.
Per channel: 16 E cells (LIF + apical), 4 co-tuned SOM (LIF inh).
Broad PV pool (32 cells, LIF) shared across all channels.
Weak nearest-neighbor E<->E ring for competition.
SFA on E cells. All weights fixed post-calibration.
"""
