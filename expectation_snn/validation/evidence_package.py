"""Per-headline-claim evidence package check (plan sec 5).

Required for each claim: direction + Cohen's d >= 0.2 + 95 pct CI
(seeds outer; trials OR cells inner, not nested; mixed-effects preferred)
+ pre-registered primary causal ablation collapse
+ held-out-seed replication on seeds {99, 314}.
"""
