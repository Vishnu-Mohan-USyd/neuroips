"""Stage 2 gate -- cue learning on H_R.

Checks:
- Cue-evoked H bump orientation-selective: cue_A -> channel 45 deg, cue_B -> 135 deg.
  d >= 0.2 with CI > 0.
- Cue alone (no stimulus) evokes H bump during delay in >= 80 pct of valid-cue trials.
- H_R recurrent weights unchanged pre/post Stage 2.
- 3 seeds.
"""
